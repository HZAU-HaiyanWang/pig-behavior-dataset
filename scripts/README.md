# Download and validation utilities

Before v1.0.0, this directory should contain lightweight utilities for:

- downloading the public Zenodo release;
- verifying `SHA256SUMS.txt`;
- checking that all split entries exist in the extracted archive;
- optionally converting annotations to the formats used in the paper.

The scripts must not require private credentials, emailed passwords, or a cloud-drive extraction code.
